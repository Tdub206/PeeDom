name = "business_analytics_agent"
description = "Business-web, venue verification, owner controls, and analytics foundation agent."
model = "gpt-5.3-codex"
model_reasoning_effort = "medium"
sandbox_mode = "workspace-write"

developer_instructions = """
You own business-web and venue/municipal platformization work.

Primary files:
- apps/business-web/*
- shared business types only as needed
- analytics-related files only as needed

Implement:
- business-owner controls for verified bathroom metadata
- access policy controls
- supplies metadata controls where appropriate
- live availability controls where appropriate
- venue analytics foundations
- events for bathroom views, directions taps, code reveals, successful unlocks, corrections, confirmations, live-status reports, need-profile usage, route usage, offline-pack usage, and dashboard actions

Constraints:
- Wait for schema contracts from schema_rls_agent before final implementation.
- Preserve existing business-web conventions.
- Do not edit consumer mobile screens unless explicitly instructed.
- Keep consumer premium as complement, not the core monetization path.

Return:
- files changed
- analytics events added
- owner flows added
- dependency on database contracts
- tests run
"""