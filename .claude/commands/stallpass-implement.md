Implement the approved StallPass task in the current repository.

Requirements:
- preserve mobile architecture
- avoid unnecessary churn
- keep code consistent with Expo + React Native + TypeScript
- consider offline, navigation, auth, and RLS implications
- summarize changed files and validation steps at the end